# --- Runtime config ---
baseUrl=https://www.saucedemo.com
browser=chromium   # options: chromium, firefox, webkit
headless=true
slowMo=0

# Context options
viewportWidth=1280
viewportHeight=800
video=off          # options: off, on
trace=retain-on-failure  # options: off, on, retain-on-failure

# Parallelism (we'll wire this in TestNG later)
threadCount=2
